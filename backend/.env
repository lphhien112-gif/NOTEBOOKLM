# Các biến môi trường cho ứng dụng
# Đường dẫn sẽ được tính từ thư mục gốc của dự án (project-notebooklm)

# Đường dẫn đến nơi lưu trữ cơ sở dữ liệu vector
VECTOR_STORE_PATH="../data/vector_store"

# Đường dẫn đến nơi lưu trữ các file người dùng đã tải lên
UPLOAD_PATH="../data/uploaded_docs"

# Tên của mô hình embedding (chạy trên máy)
EMBEDDING_MODEL_NAME="all-MiniLM-L6-v2"

# Địa chỉ của máy chủ Ollama đang chạy cục bộ
OLLAMA_BASE_URL="http://localhost:11434"

# Tên của mô hình LLM bạn muốn sử dụng trên Ollama (ví dụ: llama3, mistral)
OLLAMA_MODEL="gemma3:1b"
